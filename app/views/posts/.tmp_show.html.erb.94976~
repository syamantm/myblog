<p class="notice">
    <%= notice %>
</p>
<section>
    <article class="blogPost">
        <header>
            <h2><%= @post.title %></h2>
            <p>
                Posted on <%= @post.created_at.strftime("%B %d, %Y") %>by <%= @post.name %>
            </p>
        </header>
        <p>
            <div>
                <%= @post.content.html_safe %>
            </div>
        </p>
    </article>
</section>
<section id="comments">
    <h3>Comments(<%=  @post.comment_count%>)</h3> 
    <%= render :partial => "comments/comment",
    :collection => @post.comments %>
</section>
<h2>Add a comment:</h2>
<%= render "comments/form" %>
<br/>
<% if signed_in? %>
<%= link_to 'Edit Post', edit_post_path(@post) %> |<%= link_to 'Back to Posts', posts_path %> |
<% end %>